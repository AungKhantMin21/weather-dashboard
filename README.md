# weather-dashboard
A Node.js/Express API that fetches weather data from a public API, caches results in Redis, and serves a minimal frontend. The whole thing is Dockerized, deployed to a local Kubernetes cluster (minikube or kind), and automatically built &amp; pushed via GitHub Actions on every push to main.
